export function createPages(cars: NodeListOf<Element>) {
    if (cars.length > 10) {
        const numberOfPages = Math.floor(cars.length / 10)
        
        console.log(numberOfPages)
        const button = document.createElement('button');




    }
}